#!/usr/bin/env perl
use strict;
use warnings;

sub greet {
    my ($name) = @_;
    my $msg = qq{hello $name};
    if ($name =~ m/^[A-Z]\w+$/) {
        say $msg;
    } else {
        print q{anonymous}, "\n";
    }
    my $rx = qr/foo/i;
    $msg =~ s/foo/bar/g;
    return 1_000;
}
# trailing
